[TEMPLATE]
# Server-only secret used by /api/contact to send emails through SendGrid.
# Set this in Vercel -> Project -> Settings -> Environment Variables (Production, Preview).
# Do NOT commit the real value.
SENDGRID_API_KEY=
